# 快速开始指南

## 🚀 一键启动（推荐）

```bash
cd /home/jiaxidai/code/gift
./启动项目.sh
```

## 📋 手动启动步骤

### 1. 启动Docker服务

```bash
docker-compose up -d
```

### 2. 初始化数据库

```bash
# 等待PostgreSQL启动
sleep 5

# 创建数据库迁移
docker-compose exec backend alembic revision --autogenerate -m "Initial migration"

# 执行迁移
docker-compose exec backend alembic upgrade head

# 初始化示例数据
docker-compose exec backend python scripts/init_data.py
```

### 3. 访问服务

- **前端**: http://localhost:3000
- **后端API**: http://localhost:8000
- **API文档**: http://localhost:8000/docs

## 🔧 已实现的功能

### 后端API
- ✅ `/api/v1/products` - 商品列表和详情
- ✅ `/api/v1/categories` - 礼品分类
- ✅ `/api/v1/recommendations` - AI推荐接口

### 前端页面
- ✅ `/` - 首页
- ✅ `/recommend` - 推荐表单页面
- ✅ `/results` - 推荐结果展示
- ✅ `/products` - 商品列表页面

### 数据库
- ✅ 用户表 (users)
- ✅ 礼品分类表 (gift_categories)
- ✅ 商品表 (products)
- ✅ 评价表 (reviews)
- ✅ 评级表 (ratings)

## 📝 测试API

### 获取商品列表
```bash
curl http://localhost:8000/api/v1/products
```

### 获取分类列表
```bash
curl http://localhost:8000/api/v1/categories
```

### 获取推荐
```bash
curl -X POST http://localhost:8000/api/v1/recommendations \
  -H "Content-Type: application/json" \
  -d '{
    "recipient_type": "男/女友",
    "occasion": "生日",
    "budget_min": 100,
    "budget_max": 1000,
    "style": "浪漫型"
  }'
```

## 🐛 常见问题

### 1. 端口被占用
```bash
# 修改docker-compose.yml中的端口映射
# 或停止占用端口的服务
```

### 2. 数据库连接失败
```bash
# 检查PostgreSQL是否启动
docker-compose ps

# 查看日志
docker-compose logs postgres
```

### 3. 前端无法连接后端
```bash
# 检查环境变量
# 确保NEXT_PUBLIC_API_URL=http://localhost:8000
```

## 📚 下一步开发

1. **完善推荐算法** - 接入AI模型
2. **数据爬取** - 实现商品数据爬取
3. **评级系统** - 完善商品评级逻辑
4. **用户系统** - 添加登录和收藏功能
5. **UI优化** - 安装shadcn/ui组件库

## 💡 开发建议

- 使用Docker开发，避免环境问题
- 查看API文档：http://localhost:8000/docs
- 前后端分离，API优先开发
- 及时提交代码到Git
